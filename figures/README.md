# Figures

The validated reconstruction generated ROC, precision–recall, calibration, threshold-tradeoff, and odds-ratio plots during the analysis workflow.

The public repository keeps the tabular validation outputs in `results/` as the canonical reproducible evidence. Plot images can be regenerated from those results and the modeling pipeline when the NHANES source files are present locally.
